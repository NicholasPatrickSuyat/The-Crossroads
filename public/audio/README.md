# Audio

See `AUDIO_MANIFEST.md` for required original / licensed files.

Until those exist, `AudioManager` uses generated Web Audio placeholders
(SFX + quiet pad + regional noise). Set `AUDIO_USE_FILES` true after adding
files. Use only original or properly licensed audio — never copyrighted film scores.
